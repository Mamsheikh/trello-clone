mutation CreateColumn($boardId: String!) {
  createColumn(boardId: $boardId) {
    id
    boardId
    boardName
    columnName
    sequence
    cards {
      id
      title
      sequence
      description
      type
      bgColor
      columnId
      userId
      boardId
    }
  }
}
